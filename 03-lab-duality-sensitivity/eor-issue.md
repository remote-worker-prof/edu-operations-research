# Beads issue: будущая ЛР-03 — двойственность и анализ чувствительности

Текущая рабочая задача уже создана в Beads:

```bash
PATH="$HOME/.local/bin:$PATH" ~/.local/bin/bd show eor-prr.3
```

Если нужно создать аналогичную задачу заново:

```bash
PATH="$HOME/.local/bin:$PATH" ~/.local/bin/bd create "ЛР-03: двойственность и анализ чувствительности" \
  --type task \
  --priority 2 \
  --description "Перенести и доработать черновик лабораторной по двойственности и анализу чувствительности в отдельный будущий модуль курса." \
  --prefix eor-
```
